import { PostsTemplateProps } from "../templates/posts-template";
import { data } from "../mock/data.json";

export const postsTemplateContentMock = {
	settings: data.setting,
	posts: data.posts,
} as PostsTemplateProps;
